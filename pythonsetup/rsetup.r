
# Can add as many packages as you'd like, see data\rexamplescripts
# for more library commands
install.packages(c("tidyverse", "ggplot2", "IRkernel"))

IRkernel::installspec()  # To register the kernel in Jupyter
